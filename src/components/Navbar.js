const Navbar = () => {
    return (
        <div style={navbarStyle}>
            <h2>Navigation Bar</h2>
        </div>
    )
}

const navbarStyle = {
    textAlign: 'center',
    backgroundColor: "lightgray",
}

export default Navbar
